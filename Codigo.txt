func int fatorial(int n) {
    if (n <= 1) {
        return 1.
    } else {
        return n * fatorial(n - 1).
    }
}.

func int PRINCIPAL() {
    bool numero <- true.
    print("O fatorial do número " ++ numero ++ "é: " ++ resultado).
    return 0.
}.